import { Routes } from '@angular/router';
import { RemoveElementComponent } from './remove-element/remove-element.component';

export const routes: Routes = [
    {
        path:'',
        component:RemoveElementComponent
    }
];
